ch.x42.sling.pathdump
=====================

Sling servlet that recursively dumps all paths at and
under the current Node.

Usage example:

    curl -u admin:admin http://localhost:8080/content.pathdump.txt
